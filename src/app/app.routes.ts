import { Routes } from '@angular/router';
import { HomeComponent } from './accueil/accueil.component';

export const routes: Routes = [
  // { path: '', component: HomeComponent},
  // { path: '', component: Component},
  // { path: 'dashboard', loadComponent: () => import('./dashboard/dashboard.component').then(m => m.DashboardComponent) },
  // { path: 'control', loadComponent: () => import('./control/control.component').then(m => m.ControlComponent) },
  // { path: 'data', loadComponent: () => import('./data/data.component').then(m => m.DataComponent) },
  // { path: 'settings', loadComponent: () => import('./settings/settings.component').then(m => m.SettingsComponent) },
  // { path: '**', redirectTo: '' }
];
